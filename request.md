# Excel 操作 SKILL 与 Python 脚本需求规格说明

版本：1.0  
文档状态：需求整理稿，供后续设计与实现使用。  
输出范围：本阶段仅定义需求，不包含已实现的 SKILL 或 Python 脚本。

## 1. 项目目标

设计一套供 LLM 调用的 SKILL 及配套 Python 脚本，使 LLM 能够根据用户指令，打开指定 Excel 文件和工作表，执行隐藏行列、删除行列、写入文字、清空单元格内容以及导出 PDF 等操作。

核心原则：**除用户明确要求的修改，以及删除行列必然引起的位置与关联变化外，不得改变 Excel 原有的其他内容、格式或设置。无法可靠保留时，应停止操作并说明原因。**

“打印”在本项目中统一指 **导出 PDF**。不提供打印机选择、实体打印或打印队列管理功能。

## 2. 范围与交付物

### 2.1 必须支持的功能

1. 打开指定 Excel 文件，定位指定工作表。
2. 隐藏一个或多个指定行、列。
3. 删除一个或多个指定整行、整列。
4. 将指定工作表按指定纸张、方向、分页缩放方式导出 PDF。
5. 向指定单元格写入文字，并设置默认黑色或指定字体颜色。
6. 清空指定单元格或区域的内容。
7. 保留所有未经授权修改的格式、内容和工作簿对象。
8. 指定输出文件名、覆盖原文件，或按数字后缀自动另存为。
9. 支持按顺序执行多项操作，并返回结构化结果。

### 2.2 后续开发交付物

| 交付物 | 内容 |
| --- | --- |
| `SKILL.md` | 适用场景、参数解释、执行流程、格式保护规则、错误处理、调用示例 |
| Python 脚本 | 参数校验、Excel 操作、PDF 导出、文件命名、保存、结果输出 |
| 使用说明 | 运行环境、依赖安装、支持范围、命令行调用方法 |
| 示例操作清单 | 单项与多项操作的 JSON 示例 |
| 验证材料 | 验收用例、格式保留检查方法及已知限制 |

### 2.3 第一版边界

- 建议以 `.xlsx` 和 `.xlsm` 为第一版目标格式；处理 `.xlsm` 必须保留 VBA 宏。
- `.xls`、`.xlsb`、CSV、在线表格等不默认纳入第一版，后续另行确认。
- 写入功能以文字为范围，不包含新增公式、修改字体字号或重新设计表格样式。
- 不包含新建或删除工作表、取消隐藏、排序、筛选等未明确提出的编辑功能。
- 支持格式及默认行为以本需求为设计目标，最终支持能力须在实现和验收后确认。

## 3. 文件与工作表定位

- 输入文件通过路径指定；支持中文、日文及包含空格的文件名。
- 工作表通过完整名称明确指定，不得擅自使用当前活动工作表。
- 可以为请求指定默认工作表；单项操作若指定工作表，则以该操作的指定为准。
- 工作表不存在时，返回错误并列出可用工作表名称，不自动新建工作表。
- 文件不存在、格式不支持、文件损坏、受保护而无法操作，或目标路径无法写入时，应返回明确错误。
- 不得自动解除保护、运行宏、刷新外部数据或更新外部链接。
- 仅导出 PDF 时，不得为此改写原 Excel 文件。

## 4. 行列与单元格的指定规则

### 4.1 行列选择

| 对象 | 示例 | 含义 |
| --- | --- | --- |
| 单行 | `5` | 第 5 行 |
| 多行 | `5,8,12` | 第 5、8、12 行 |
| 连续行 | `5:10` | 第 5～10 行，包含两端 |
| 混合行 | `5,8:12,20` | 第 5 行、第 8～12 行及第 20 行 |
| 单列 | `C` | C 列 |
| 多列 | `C,F,H` | C、F、H 列 |
| 连续列 | `C:F` | C～F 列，包含两端 |
| 混合列 | `B,D:F,J` | B 列、D～F 列及 J 列 |

- 行号从 1 开始，与 Excel 显示一致。
- 列使用字母表示，不区分大小写。
- 同一操作中的重复或重叠范围自动合并。
- 空选择、反向范围、非法字符、越界地址必须报错，不猜测用户意图。
- JSON 参数可使用数组表达多个选择；具体字段在接口设计时固定。

### 4.2 单元格选择

- 使用 A1 地址形式，例如 `A1`、`C12`。
- 清空操作支持多个地址及矩形区域，例如 `A1,C3`、`B2:D10`。
- 写入操作支持多个“地址—文字—颜色”条目，为不同单元格写入不同内容。
- 不默认将同一文字填充到整个区域；批量填充若需要支持，应单独明确参数语义。

### 4.3 坐标解释

- 各项操作按给定顺序执行。
- 后续操作中的坐标，以此前操作完成后的工作表状态为准。
- 同一次删除操作中的所有行列坐标，以该次删除开始前的状态为准。
- 不得为了执行方便，擅自把写入、隐藏、删除等操作重新排序。

## 5. 隐藏行列

- 支持隐藏单行、多行、单列、多列、连续范围及混合范围。
- 只修改目标行列的隐藏状态。
- 保留其内容、公式、样式、原有行高或列宽参数及分组设置。
- 已隐藏的行列再次隐藏，应视为成功且无需额外修改。
- 不得将隐藏解释为删除、清空或将行高、列宽永久改写为零。

## 6. 删除行列

- 删除对象为整行或整列，不是清空内容或局部移动单元格。
- 删除后，其余单元格按 Excel 的正常结构操作语义移动。
- 多个不连续行列的删除必须避免因索引变化误删；具体执行策略由脚本保证。
- 保留剩余单元格随位置移动的原有格式，不重建工作表，不批量重新套用样式。
- 公式引用、名称定义、合并区域、表格、条件格式、图表、打印区域等涉及被删行列时，应按正常结构删除语义处理。
- 删除导致的必要引用变化允许发生；可能产生的失效引用应尽可能在执行结果中说明，不得自动猜测修复。
- 若执行引擎不能可靠维护相关结构，应在提交结果前停止，不得静默丢弃对象或输出可能损坏的文件。

## 7. 写入文字与字体颜色

### 7.1 文字写入

- 支持向一个或多个单元格写入文字，替换目标单元格原有内容。
- 必须按文字写入：`00123` 保留前导零，`=ABC` 保留为文字而非公式。
- 支持中文、日文、英文、符号和换行字符。
- 不修改数字格式来强制文本化；实现方式必须同时满足文字语义与格式保留要求。
- 不自动调整行高、列宽、自动换行或对齐方式。文字是否完全显示取决于原有布局。
- 空字符串写入仍遵循写入及颜色规则；仅希望清空内容时，应使用清空操作。

### 7.2 颜色设置

- 未指定颜色时，字体颜色默认为黑色。
- 支持预定义颜色名称及六位 RGB HEX。
- 颜色名称不区分大小写；HEX 支持带或不带 `#`。
- 非法颜色必须报错，不自动回退为黑色。
- 仅修改目标单元格的字体颜色，不重建或整体替换字体属性。

建议固定以下颜色映射：

| 名称 | RGB HEX |
| --- | --- |
| `black` | `#000000` |
| `white` | `#FFFFFF` |
| `red` | `#FF0000` |
| `green` | `#008000` |
| `blue` | `#0000FF` |
| `yellow` | `#FFFF00` |
| `orange` | `#FFA500` |
| `purple` | `#800080` |
| `gray` / `grey` | `#808080` |

本功能修改基础字体颜色。若条件格式覆盖显示颜色，应保留条件格式并说明该情况，不得删除规则以强制显色。

### 7.3 格式与特殊单元格

- 保留字体名称、字号、加粗、斜体、下划线、边框、填充、对齐、数字格式等属性。
- 合并单元格仅允许通过左上角主单元格写入；指定其他位置时返回错误及主单元格地址。
- 富文本、数组公式区域等特殊结构若无法满足局部修改和格式保护要求，应报错，不隐式转换为普通单元格。

## 8. 清空单元格内容

- 支持单个单元格、多个不连续单元格、一个或多个矩形区域。
- 删除目标中的文字、数值或公式。
- 不删除单元格，不移动其他内容。
- 保留字体及颜色、数字格式、边框、填充、对齐、条件格式、数据验证、批注、超链接和合并状态。
- 已为空的单元格再次清空，视为成功。
- 合并区域的内容以主单元格为准：目标包含主单元格时可以清空；仅涉及非主单元格时返回明确错误，不扩展范围。
- 对数组公式等不可独立清空的结构，应报错，不擅自扩大清空范围。

## 9. 导出 PDF

### 9.1 基本要求

- 每项导出操作必须明确一个目标工作表，仅导出该工作表。
- 不使用打印机名称、打印机选择界面或实体打印任务作为用户接口。
- 支持单独导出 PDF，也支持编辑完成后导出 PDF。
- 编辑后的 PDF 应反映本次操作完成后的工作表状态。
- 第一版不要求多个工作表合并成一个 PDF。

### 9.2 页面参数

| 参数 | 支持值或含义 | 未指定时 |
| --- | --- | --- |
| 纸张大小 | 至少支持 A4、A3、Letter；其他尺寸按引擎支持范围声明 | 保持工作表原设置 |
| 纸张方向 | 纵向、横向 | 保持工作表原设置 |
| 缩放方式 | 保持原设置、所有列一页、所有行一页、全部内容一页 | 保持原设置 |
| 打印区域 | 明确指定的单元格区域 | 使用原打印区域；未设置时采用工作表正常打印范围 |
| PDF 路径 | 用户指定的文件名或完整路径 | 按第 11 节生成 |
| 保存页面设置 | 是否将本次覆盖的页面设置写入输出 Excel | 否，仅用于本次 PDF |

### 9.3 缩放方式的精确定义

| 模式 | 宽度 | 高度 |
| --- | --- | --- |
| 保持原设置 | 不修改 | 不修改 |
| 所有列放进一页 | 1 页 | 不限制页数 |
| 所有行放进一页 | 不限制页数 | 1 页 |
| 全部内容放进一页 | 1 页 | 1 页 |

“一页”约束针对实际导出区域。所有列一页并不意味着整个工作表只有一页。缩放不得通过修改 Excel 字号、行高或列宽实现。

### 9.4 保留规则与异常

- 未指定的页边距、页眉页脚、重复打印标题、打印网格线等设置保持原样。
- 显式选择适配一页时，允许由相应缩放设置产生必要分页变化；不额外改写无关分页配置。
- 隐藏行列按工作表正常打印行为处理，不为了导出而取消隐藏。
- 默认仅临时应用本次页面参数，不污染原文件或另存的 Excel 页面设置。
- 只有明确要求保存页面设置时，才把这些修改作为授权变更写入输出 Excel。
- 对隐藏工作表或没有可打印内容的情况，返回明确结果，不擅自取消隐藏或伪造成功输出。
- 缺少 PDF 导出能力、渲染失败、文件无法生成时，返回错误，不静默使用可能改变布局的替代方案。
- PDF 成功标准包括文件可读取且具有有效页面；是否满足页面布局还应在验收中检查。

## 10. 格式及工作簿保护

### 10.1 允许变化的边界

| 对象或属性 | 允许变化的情况 |
| --- | --- |
| 单元格内容 | 明确写入、清空或删除 |
| 基础字体颜色 | 写入时默认黑色或用户指定颜色 |
| 行列隐藏状态 | 明确要求隐藏 |
| 行列位置、引用和关联范围 | 删除行列所必需的结构变化 |
| 页面设置 | 明确指定的项目，按临时应用或保存范围生效 |
| 其他内容、格式、对象与设置 | 必须保留 |

### 10.2 保护对象

保护范围包括但不限于：

- 未操作的单元格及工作表。
- 字体、字号、强调样式、边框、填充、对齐、数字格式。
- 行高、列宽、合并区域、分组、冻结窗格和筛选设置。
- 条件格式、数据验证、批注、超链接及名称定义。
- 公式、表格、图片、图表、形状、数据透视表及其他原有对象。
- VBA 宏、工作表顺序、可见性和未经授权变更的打印设置。

因删除产生的合法关联变化按第 6 节处理，不能据此允许无关变化。

### 10.3 保证方式

- 格式保留是实现和验收的硬性要求，不是“尽量接近”的目标。
- 不允许通过重建工作簿、复制纯数据或移除不支持对象来完成任务。
- 保存后的文件不要求二进制字节完全一致，但其内容、样式、对象和行为必须符合授权变化边界。
- 不主动触发无关的公式重算、外部刷新或宏执行；若环境会引起无法控制的额外变化，应说明并停止。
- 实现前应声明引擎、文件类型和对象的支持范围；对无法可靠处理的文件拒绝提交。

## 11. 输出文件与命名规则

### 11.1 Excel 保存模式

| 模式 | 行为 |
| --- | --- |
| 默认另存为 | 在输入文件所在目录生成带数字后缀的新文件 |
| 指定输出 | 使用指定文件名；仅给文件名时保存到输入文件所在目录 |
| 覆盖原文件 | 仅在明确指定覆盖模式时执行 |

- 默认保留原 Excel 扩展名，不隐式转换格式。
- 指定输出文件已存在时，默认报错；覆盖该目标需明确授权。
- 指定输出路径与输入相同时，必须同时明确选择覆盖原文件。
- 冲突或互斥的保存参数应报错，不静默选择其中一个。

### 11.2 自动数字后缀

| 输入文件 | 默认输出候选 |
| --- | --- |
| `报告.xlsx` | `报告_01.xlsx` |
| `报告_01.xlsx` | `报告_02.xlsx` |
| `报告_09.xlsx` | `报告_10.xlsx` |
| `报告_99.xlsx` | `报告_100.xlsx` |
| `报告_01_草稿.xlsx` | `报告_01_草稿_01.xlsx` |

- 仅将扩展名前、主文件名末尾的 `_数字` 识别为版本后缀。
- 无后缀时从 01 开始；有后缀时从该数字加 1 开始。
- 后缀至少两位；原后缀位数更长时保留其位数，进位时自然扩展。
- 若候选已存在，逐个递增至第一个可用名称，不覆盖已有文件。
- 文件名中的其他数字保持不变。

### 11.3 PDF 文件命名

- 支持独立指定 PDF 文件名或路径；仅给文件名时使用输入 Excel 所在目录。
- 编辑并另存 Excel 时，默认使用输出 Excel 的主文件名加 `.pdf`，放在输出 Excel 所在目录。
- 仅导出 PDF，或覆盖原 Excel 后导出时，以输入 Excel 主文件名按同样的后缀递增规则生成 PDF 名称。
- 自动命名遇到已有 PDF 时继续递增；明确指定的 PDF 路径已存在时，默认报错。
- 只导出 PDF 时不额外生成 Excel 副本。

### 11.4 保存可靠性

- 先写入临时文件，完成必要验证，再提交最终输出。
- 覆盖原文件必须保证失败时不破坏原文件；目标被占用时返回错误。
- 不覆盖用户未授权覆盖的任何文件，包括命名检查后被其他进程创建的文件。
- 若执行期间发现输入文件已被其他程序修改，应停止覆盖，不丢弃外部修改。

## 12. 批处理与失败处理

- 一次请求对应一个输入工作簿，可包含多个目标工作表上的有序操作。
- 先检查参数、工作表、权限及可识别的兼容性问题，再执行修改。
- 某操作的坐标或结构检查依赖前序操作时，在相应临时状态下验证。
- 默认所有编辑必须成功，才允许提交最终 Excel 文件。
- 编辑及 PDF 导出均应尽可能先在临时输出中完成；预提交失败时不交付半成品。
- 多文件最终提交若发生部分成功，必须分别报告已提交与未提交文件，不得宣称整批成功或保证并不存在的跨文件原子性。
- 重试不得重新应用已完成的删除操作；应基于明确的输入状态重新执行，或仅重试失败的输出步骤。
- 原因已明确的错误不进行无意义重试，不以降级格式保留要求换取“成功”。

## 13. SKILL 与脚本的职责划分

### 13.1 SKILL

- 把自然语言请求转换为明确的结构化操作清单。
- 缺少必要信息时只询问真正影响执行的参数；可使用本文件规定的默认值。
- 在需要时向用户解释删除后的坐标含义和格式保护边界。
- 不擅自推断目标工作表、修改范围或覆盖权限。
- 调用脚本，检查结构化结果，并准确报告生成文件及失败项目。

### 13.2 Python 脚本

- 接收 JSON 操作清单，不依赖自然语言猜测。
- 严格校验字段、数据类型、地址、颜色、文件路径及互斥参数。
- 负责实际编辑、格式保护、PDF 导出、命名、保存与错误处理。
- 接口应可重复调用，适合命令行与 LLM 工具集成。
- 未知操作或未知参数应报错，避免拼写错误被静默忽略。
- 成功和失败都返回可解析的 JSON；调试日志不得混入结构化结果。

### 13.3 返回结果的最低内容

| 字段类别 | 应包含的信息 |
| --- | --- |
| 总体状态 | 成功、失败、部分提交 |
| 输入信息 | 输入文件路径 |
| 操作结果 | 操作顺序、类型、工作表、目标范围、状态 |
| 输出信息 | 实际生成的 Excel、PDF 路径及提交状态 |
| PDF 信息 | 导出工作表、实际页面参数、页数（可获取时） |
| 错误信息 | 稳定错误代码、中文说明、出错步骤 |
| 注意事项 | 必要的引用变化或显示颜色受条件格式影响等情况 |

## 14. 验收要求

| 编号 | 验收场景 | 预期结果 |
| --- | --- | --- |
| A01 | 指定文件和工作表 | 只操作准确匹配的目标 |
| A02 | 隐藏单个、多个及重叠行列范围 | 目标正确，内容及原有样式保留 |
| A03 | 删除多个不连续行列 | 无索引偏移误删，剩余单元格格式随位置保留 |
| A04 | 写入普通文字、`00123`、`=ABC` | 均按文字保存，不丢前导零、不形成公式 |
| A05 | 默认颜色、名称颜色及 HEX | 只修改授权字体颜色，其他字体属性保留 |
| A06 | 清空文字、数值及公式 | 内容移除，格式和附加对象按要求保留 |
| A07 | 合并单元格及不支持的特殊结构 | 按既定规则处理或报错，不扩大修改范围 |
| A08 | 四种 PDF 缩放模式 | 输出页宽、页高约束符合定义 |
| A09 | 指定纸张和方向 | PDF 页面尺寸及方向正确 |
| A10 | PDF 临时页面设置 | PDF 使用指定设置，Excel 原页面设置不变 |
| A11 | 自动后缀、已有后缀及名称冲突 | 按规则递增，不覆盖已有文件 |
| A12 | 指定输出和覆盖原文件 | 仅覆盖被明确授权的目标 |
| A13 | 任意编辑步骤失败 | 不提交部分编辑结果，原文件完整 |
| A14 | 多步删除、隐藏、写入组合 | 顺序与坐标语义一致 |
| A15 | 包含宏、图片、图表等对象的文件 | 在声明支持范围内保留对象；不支持时明确拒绝 |
| A16 | 仅导出 PDF | 不改写原 Excel，不额外生成 Excel 副本 |
| A17 | 多输出提交中途失败 | 准确报告每个实际提交结果，支持避免重复编辑的重试 |

验收应使用包含不同格式与对象的代表性工作簿，比较修改前后未经授权变更的属性，并检查 PDF 页面。不能仅以文件能打开或脚本退出码为零判断格式保留合格。

## 15. 实现前待确认事项

以下事项影响技术选型，不影响本需求文档作为功能规格使用：

1. 脚本运行平台：Windows、macOS 或 Linux。
2. 运行环境是否安装桌面版 Microsoft Excel，以及是否允许脚本调用。
3. 是否接受第一版以 `.xlsx`、`.xlsm` 为目标，是否需要额外文件格式。
4. 典型工作簿是否包含复杂图表、宏、外部链接、富文本、数据透视表等对象，以确定支持范围及验收样本。

最终实现必须公开其运行前提和支持边界，不得将“保留所有格式”的要求替换为未经验证的兼容性承诺。
